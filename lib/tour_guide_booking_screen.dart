import 'package:flutter/material.dart';

class TourGuideBookingScreen extends StatelessWidget {
  const TourGuideBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tour Guide Booking')),
      body: const Center(child: Text('Tour guide booking will be implemented with database.')),
    );
  }
}